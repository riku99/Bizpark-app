import { Box } from "native-base";
import React from "react";
import { AddButton } from "src/components/AddButton";

export const MyPage = () => {
  return (
    <Box flex={1}>
      <AddButton />
    </Box>
  );
};
